function getPerimeterCircle(r) {
    return 2 * Math.PI * r;
}

function getAreaCircle(r) {
    return Math.PI * r * r;
}

document.writeln(getPerimeterCircle(5) + "<br>");
document.writeln(getAreaCircle(5) + "<br>");
